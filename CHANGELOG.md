# 变更记录

本文件记录 ScopeLatch 的重要变更。

## 未发布

## 4.0.0 - 2026-07-30

### 重大变更

- 将 V4 设为唯一活动运行时，移除 V3 兼容 adapter、状态升级和正式判定分支。
- 验证结果改为闭合 outcome；缺失、跳过、旧 status 或不可验证证据不再可被提升为 PASS。
- 引入内容寻址证据链、候选 Git 身份绑定、可执行不变量目录和独立 Auditor。
- 引入并发原子状态、安全执行器、冷/缓存复现和受保护基线 Judge。
- 新增非权威 `Harness v4 Shadow` 与失败关闭的 `Harness v4 Formal Attestation` 工作流。

### 迁移与安全

- 安装器同步 V4 Auditor、契约、执行器、资格工具和 Hook。
- GitHub Actions 使用固定提交 SHA 和固定 Node/Python 版本，最小权限且不向候选暴露写权限或 secrets。
- GitHub checkout 禁止持久化临时凭据，工作流表达式不再直接插入 PowerShell 命令文本。
- Codex 启动器和依赖审计统一使用 `shell: false` 的可执行文件/参数数组；安装器拒绝穿过符号链接或目录联接写出目标仓库。
- 原项目 H8/H9 远程观测不迁移；本仓库从 `0/20` 重新积累资格窗口，Formal 保持阻断。
- 通用安装模板中的验证能力默认为 `false`，要求用户显式配置结构化命令。

### 文档与证据

- 更新从无 Harness、初版原型、项目实测优化到 ScopeLatch 4.0.0 的效果对照。
- 发布机器可读的历史聚合数据和 CI 验证脚本；明确区分能力覆盖、运行档案与尚未测量的效率指标。

## 3.4.0 - 2026-07-23

### 新增

- 新增任务显式写入目标 `writeTargets`，将阅读上下文与写入授权分离。
- 新增停止 Hook 自动收尾，可自动完成 Guard、验证、二次 Guard 和 PR 报告。
- 新增严格范围、自动收尾和安装后策略的回归测试。

### 调整

- 将项目名称、CLI、安装器、Docker 沙箱镜像和公开元数据统一为 ScopeLatch。
- 重排公开文档，补充快速开始、目录说明和安全边界。
- 将维护者发布清单移动到 `docs/maintainers/`，减少根目录噪声。

### 清理

- 删除尚无有效记录的稳定失败规则与规则审核占位文件；后续产生真实审核记录时再由 ScopeLatch 创建。

### 修复

- 修复 `.json` 被截断为 `.js`、`.tsx` 被截断为 `.ts` 的任务路径解析问题。

### 安全

- 默认禁用 `--allow-out-of-scope`，遗漏目标必须重新规划。
- 领域同步、反向依赖和相关测试不再自动授权目录级写入。

## 3.3.0 - 2026-07-22

### 新增

- 与任务指纹、会话、分支、提交、基线、有效期和影响范围绑定的写入租约。
- `PostToolUse` 副作用检查和完整 Codex 进程收尾。
- 权威源码清单与阶段无关的验证入口。
- 结构化失败签名、候选审核和稳定规则提升机制。
- 面向公开发布的当前文件、Git 历史、依赖、本机路径和许可证扫描。
- 强化 Docker 沙箱验证和显式写入授权。
- `PostToolUse` P50/P95 性能基准与哈希缓存。
- 独立安装器、中文 README、公开 CI 和开源治理文件。

### 调整

- 从原应用仓库中提取为独立工程控制系统。
- 用可移植的仓库默认值替换项目专用路径与配置。

### 安全

- 安全报告只保存发现类型和文件位置，不保存检测到的凭据原文。
- Codex Hook 会拒绝危险命令、过期或不匹配的写入租约，以及未完成收尾前的提交。
